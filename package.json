{
  "name": "lantern-journal",
  "version": "1.0.0",
  "description": "A user-authenticated AI journaling app built for the Google Cloud Run & AI Challenge (#AccelerateAIwithCloudRun). Firebase Auth + Firestore + Gemini, served from Cloud Run.",
  "main": "server.js",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "echo 'Build complete'",
    "start": "node server.js",
    "dev": "node server.js",
    "lint": "node --check server.js"
  },
  "dependencies": {
    "@google/genai": "^0.15.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.6.0"
  },
  "license": "MIT"
}
